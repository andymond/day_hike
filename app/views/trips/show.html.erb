<% @trip.trails.each do |trail| %>
   <div class="trip-trails">
    Trail: <%= trail.name %>
    Address: <%= trail.address %>
    Length: <%= trail.length %>
   </div>
<% end %>

Total Planned Length:
<%= @trip.total_trail_length %>
<%= @trip.avg_trail_length %>
<%= @trip.longest_trail %>
<%= @trip.shortest_trail %>
